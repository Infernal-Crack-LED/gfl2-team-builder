# Front-end conventions — gfl2-team-builder

> **Class: CURRENT-STATE.** Freely rewritten when the code moves; stale content
> is deleted, not narrated.
>
> **Audience:** anyone — human or AI, any harness — writing or reviewing
> front-end code in `web/`. This doc is **binding**: where it and your instinct
> disagree, check the code, and if the code agrees with the doc, follow the doc.
>
> **Lineage:** this doc is the GFL2 instance of the nikke-sim website playbook
> (`~/nikke-sim/docs/frontend-conventions.md`). Patterns marked **[playbook]**
> are carried over verbatim from there; that doc holds the long-form rationale,
> incident history, and the sections this site hasn't grown into yet (thumbnail
> pipelines §10).
> When this site adopts one of those sections, port its rules here and delete
> the pointer.

---

## 1. Architecture

| Concern        | Approach                                                                                                                                                                                                         |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Framework      | React 18, functional components only, hooks only **[playbook]**                                                                                                                                                  |
| Build          | Vite 5 (`@vitejs/plugin-react`), `root: 'web'`, output to repo-root `dist/` **[playbook]**                                                                                                                       |
| Language       | TypeScript strict (`web/tsconfig.json`); `npm run typecheck` checks web AND the sync/server code                                                                                                                 |
| Routing        | Custom SPA router (`web/src/router.ts`) — real URL **paths**, `pushState` + `popstate`. No React Router, never hash routing **[playbook]**                                                                       |
| State          | `useState` / `useReducer` + prop drilling. No global store, no context for state **[playbook]**                                                                                                                  |
| Styling        | One CSS file (`web/src/styles.css`). No CSS modules, no Tailwind, no styled-components **[playbook]**                                                                                                            |
| Data           | JSON imported directly at build time (`import dollsJson from '../../data/dolls.json'`). All game data runs **entirely client-side** — no data fetch **[playbook]**                                               |
| Data origin    | Postgres, filled by the dandegate sync (`npm run sync`). The sync step **exports** the committed `data/*.json` artifacts the web imports — the DB is the source of truth, the JSONs are build inputs (see §8)    |
| Code splitting | Every page is a `lazy()` route chunk; React is its own `manualChunks` framework chunk **[playbook]**                                                                                                             |
| SEO / embeds   | Static `index.html` baseline + client head sync + **server-side per-URL embed injection** (meta, breadcrumbs, no-JS bodies, hard 404s) — §6                                                                      |
| Infographics   | Server-rendered share cards in `src/infographics/` (playbook §9 adopted — see §12): platform-free core renderers, @napi-rs/canvas + sharp on the node side, content-addressed immutable URLs via `/api/v1/img/*` |
| Images         | CDN URLs (`cdn.dandegate.net`) with `loading="lazy"` + `?` placeholder; the playbook's thumbnail-tier pipeline (§10) arrives with real assets                                                                    |

### File layout

```
web/
  index.html                  # SPA shell: static head (title/OG/twitter/canonical/JSON-LD) + empty #root
  tsconfig.json
  public/
    site-icon.png             # shared logo (copied from nikke-sim) — favicon + nav mark
    og.png                    # default OG share image (placeholder until designed)
    robots.txt                # named social-crawler groups + AI allowlist (§6)
    sitemap.xml               # GENERATED by `npm run sitemap` — never hand-edited
    llms.txt                  # hand-written site map for answer engines (§6)
    gfl2-icons/               # filter/card icons — added when assets land (§10)
    img/portraits/            # build-time portrait tiers — phase 2 (§10)
  src/
    main.tsx                  # React root: lazy() page switch, Suspense fallback, head sync
    router.ts                 # path-based SPA router (§4)
    SiteChrome.tsx            # SiteNav + SiteFooter (shared chrome on every page)
    styles.css                # ALL styles (§3)
    useDocumentHead.ts        # per-route client-side <head> sync (§6)
    jsonLd.ts                 # escapeJsonLd helper (</script> breakout prevention)
    data.ts                   # typed imports of data/*.json + slug/id resolution
    auth.ts                   # Discord auth + saved-profiles client — the ONLY runtime API (§7)
    components/
      DollGrid.tsx            # useDollFilter + DollFilters + DollCards (§5)
      WeaponGrid.tsx          # the weapon-grid analog
      SaveProfileControl.tsx  # save/load saved-profiles control (uses auth.ts)
      InlineNameField.tsx     # name input + confirm/cancel for naming a save
    CharactersPage.tsx  DollPage.tsx  WeaponsPage.tsx  WeaponPage.tsx
    TeamBuilderPage.tsx  DollBuilderPage.tsx  HomePage.tsx  CreditsPage.tsx
    buildShare.ts           # /builder/<slug> share-link boot helpers (?b= / ?id=)

data/                         # committed, generated by `npm run sync` — NEVER hand-edited
  dolls.json  weapons.json  keys.json  effects.json

src/                          # non-web code (sync, db, server) — existing
  db/  sync/  bin/
  bin/build-sitemap.ts        # sitemap.xml generator (routes + every doll/weapon)
  share/pageMeta.ts           # THE per-URL title/description/label table (§6)
  server/                     # Hono app: static dist/ + OAuth + profiles API (§7)
    pageMeta.ts               # URL → page + meta/breadcrumb/404 injection (§6)
    noJsBody.ts               # no-JS bodies for the crawl surfaces (§6)
    ogInject.ts               # share-card OG override, layered on top (§12)
    htmlHead.ts  httpCache.ts # head-tag rewriting; cache classes + ETag/304
```

---

## 2. TypeScript conventions **[playbook]**

### Imports

```ts
// React — named imports, type-only for event types
import { useEffect, useState, useMemo } from 'react';
import type { MouseEvent, ReactNode } from 'react';

// JSON data — default import with a two-step cast
import dollsJson from '../../data/dolls.json';
const data = dollsJson as unknown as DollsFile;
```

### Component signatures

```ts
// Named exports ONLY (never default). Props typed inline or via interface.
export function ComponentName({ prop1, prop2 }: { prop1: string; prop2?: number }) { ... }
```

Lazy pages adapt at the import site, not by adding default exports:

```ts
const CharactersPage = lazy(() =>
  import('./CharactersPage').then((m) => ({ default: m.CharactersPage }))
);
```

### Typing patterns **[playbook]**

- Union types for finite sets: `type Route = 'characters' | 'character' | …`
- `as const` for fixed arrays/tuples (filter option lists)
- `Record` types for lookups (`PHASE_COLORS`, label maps)
- Discriminated unions for result state

### Custom hooks **[playbook]**

`useXxx` naming. Exported hooks are shared infrastructure (the filter hook is
THE example — §5); internal ones stay file-local.

### Comments **[playbook]**

Extensive and explanatory: WHY decisions were made, invariants, edge cases,
cross-references. The filter module's OR-within-row / AND-across-rows
semantics is exactly the kind of rule that gets a comment.

---

## 3. Styling — one CSS file, token-driven **[playbook]**

Everything lives in `web/src/styles.css`. Do not add a second CSS file, CSS
modules, Tailwind, or styled-components. New feature styles go under a
`/* ---- Feature ---- */` banner with a shared class prefix.

### Design tokens (identical to nikke-sim — owner decision 2026-08-15)

```css
:root {
  color-scheme: dark;
  --bg: #101216; /* page background */
  --panel: #181b22; /* card/panel background */
  --panel2: #1f232d; /* elevated/sunken surface (inputs, alternate rows) */
  --border: #2a2f3b; /* borders, dividers */
  --text: #e7eaf0; /* primary text */
  --muted: #8b93a3; /* secondary text, labels, placeholders */
  --accent: #5b9dff; /* interactive blue (buttons, active states, links) */
  --warn: #e0b04b; /* warning/caution yellow */
}
```

All colors reference these tokens. Game-phase accent colors (GFL2's analog of
nikke-sim's `ELEMENT_COLORS`) live in a TS const when first needed — see the
scaffolding plan §6 for the proposed `PHASE_COLORS` values.

### Fonts

System stack only (`14px/1.5 -apple-system, BlinkMacSystemFont, 'Segoe UI',
Roboto, sans-serif`). Browser CSS still self-hosts NOTHING; the Roboto TTFs
under `src/infographics/assets/fonts/` exist for the server-side canvas
renderers only (§12), registered under the family name `Roboto` so canvas
text resolves weights the same way the site's stack does.

### Typography, spacing, radius, shadows **[playbook]**

- Base 14px · h1 22–28px · h2 15–18px · labels 11px uppercase muted ·
  `tabular-nums` on numeric columns
- 2px-based spacing scale; card padding 10–16px; page padding
  `24px 20px 60px` desktop / `14px 8px 48px` mobile
- Radius: pills 999px · cards 10px · modals 12px · inputs/buttons 8px ·
  portraits 6–8px
- Shadows always dark and subtle (`0 8px 24px rgba(0,0,0,.5)` dropdowns,
  `0 12px 40px` modals)

### Layout, buttons, inputs, tables **[playbook]**

`.app` page container (max-width 1180px); `.card` panel pattern. Buttons,
ported from nikke-sim: `.btn-primary`/`.btn-outline` share one outline voice
(panel2 surface, accent ring, hover fills accent with `--accent-ink` text;
`.discord` variant reserved for login); toggle pills as `.pills button` or
the per-button `.pill-toggle` (`.on` = accent fill, `.pills.small` denser,
16px `.pill-icon`); `.chip` small action chip (hover flips to `--warn`).
Inputs/selects/textareas share one recipe (panel2, 8px radius, `6px 9px`).
Popovers (`.popover-panel`), underline tabs (`.tabs-bar`), and the modal
(`.modal-backdrop` + `.modal-panel`, max-width 520px) follow the nikke-sim
recipes; collapse tables with uppercase muted headers.

### Responsive breakpoints **[playbook]**

900 / 760 / 720 / 640px as in the playbook; 640 is the main mobile breakpoint
(nav → dropdown). Prefer content-aware layout (`ResizeObserver`) over
breakpoints; never `window.addEventListener('resize')`.

### Accessibility baselines (site-wide, in styles.css) **[playbook]**

```css
:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
@media (prefers-reduced-motion: reduce) {
  /* kill animations/transitions */
}
```

Icon-only controls carry `aria-label` + `.sr-only` text twin; filter toggles
use `aria-pressed`; panels use `role="menu"`/`role="alertdialog"` where the
playbook does.

---

## 4. Routing and navigation **[playbook]**

Every view is addressed by a real URL **path** (never a hash) — paths reach the
server and are independently crawlable.

### Route model (`web/src/router.ts`)

- `Route` union: `home | characters | character | weapons | weapon |
team-builder | builder | credits`.
- Parameterized routes (`/characters/<slug>`, `/weapons/<slug>`,
  `/builder/<slug>`) are excluded from the flat `ROUTES` list —
  `hrefFor('character')` would produce a slug-less dead end. Detail routes
  are detected by a present second segment. `/builder` without a slug lands
  on `characters` (a builder without a doll is meaningless).
- `navigate()` = pushState + synthetic popstate; `useRouteAndSlug()` is the
  single subscription route and slug derive from; scroll-to-top only when the
  route actually changes.

### Links are real anchors, intercepted **[playbook]**

```tsx
<a href={href} onClick={onSpaLinkClick(href)} className={current === route ? 'on' : ''}>
```

Modified clicks (⌘/middle) behave natively. Render real `href`s always —
crawlability and middle-click depend on it. The `/characters` and `/weapons`
grids are navigation-mode card grids precisely so every card is a crawler-
followable `<a>`.

---

## 5. Component patterns

### The filter module (`web/src/components/DollGrid.tsx`)

The central shared component, structurally identical to nikke-sim's
`CharacterGrid.tsx` split:

| Export                                   | Role                                                                                                                                                                                                     |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `useDollFilter({ exclude?, restrict? })` | owns ALL filter/search state + the derived doll list + any thumbnail maps. Split from rendering so a caller can place controls and grid separately (the Team Builder puts filters above the squad strip) |
| `DollFilters`                            | `<details>` panel + search box + "Showing N of M" + Clear all. `defaultOpen`: true on `/team-builder`, false on `/characters`                                                                            |
| `DollCards`                              | the grid. **Navigation mode** (`linkFor` → whole card is an `<a>`) vs **badge mode** (click = place in squad; separate corner profile link)                                                              |

Filter semantics (binding, from the playbook): **OR within a row, AND across
rows**; an empty row is inactive (all values pass); text search matches name
and `searchTags` aliases. Filter rows: Class, Phase, Ammo, Weapon type
(icon-capable rows) + Rarity (text pills) — vocabulary defined as
`{ id, label, icon? }` option arrays so icon assets drop in later without a
code change.

The rule that makes this worth having: `/characters` and `/team-builder` use
the SAME hook instance shape, so the filter vocabulary can never drift between
the two pages.

### Page structure **[playbook]**

```tsx
export function SomePage() {
  return (
    <div className="app page-class">
      <header>
        <h1>Page title</h1>
        <p className="muted">Introductory text…</p>
      </header>
      <section>{/* content */}</section>
    </div>
  );
}
```

Detail pages (DollPage, WeaponPage) derive EVERYTHING from the committed
`data/*.json` — never hand-write per-entity blurbs — and **degrade, never
vanish**: a section without data says so in one muted line instead of
disappearing.

### Dropdown/modal dismiss pattern (site-wide) **[playbook]**

Outside-`mousedown` + `Escape`, attached while open, cleaned up on close —
identical shape everywhere a popover exists.

### Portrait/image `<img>` pattern (current phase)

All character/weapon art runs through ONE CSS frame (nikke-sim pattern):

```tsx
<img className="portrait" src={doll.avatarUrl} alt={doll.name} loading="lazy" />
```

`.portrait` = square (`aspect-ratio: 1`), `object-fit: cover` anchored at
`center var(--portrait-crop-top)` (16%, the CSS stand-in for nikke-sim's
`PORTRAIT_CROP_TOP` — keep them in sync if a thumbnail pipeline lands),
panel2 background, border + 8px radius. Inside `.dollcard`/`.weaponcard`/
`.teambuilder-slot` the card frame supplies the border, so the rule strips
the portrait's own. Size variants: `.unit-portrait` (96×96, detail headers),
`.portrait-sm` (26×26, picker rows), `.portrait-contain` (weapon art is
512×256 with transparency — letterbox, don't crop). Missing URLs keep the
`.portrait-empty` `?` placeholder. No thumbnail tiers yet — the playbook §10
pipeline (stepped-halving downscale, build-time tiers, one crop constant) is
adopted when real art assets land. Do NOT port it speculatively.

---

## 6. SEO and social embeds

**The head baseline:** static `index.html` (title/description/keywords/
canonical/OG 1200×630/twitter/theme-color/WebApplication JSON-LD) +
`useDocumentHead.ts` per-route client sync; detail pages set their own head via
`setDetailMeta` so the full dataset never lands in the eager entry chunk.

**Per-URL embed system (playbook §6–7, adopted 2026-08-17).** Crawlers run no
JS, so the server bakes each URL's head into the HTML it returns. Full write-up:
[scaffolding-plan.md §12](scaffolding-plan.md). The rules that bind new work:

- **The copy lives in ONE place** — `src/share/pageMeta.ts` (`ROUTE_META` +
  `dollPageMeta`/`weaponPageMeta`/`builderPageMeta`), imported by both the
  client head sync and `src/server/pageMeta.ts`. The playbook's lockstep rule
  ("route meta lives in ALL tables at once") is structural here: there is only
  one table. **Never** hand-write a title/description at a call site — add or
  extend an entry and let both sides read it.
- **Adding a page means three edits**: a route in `web/src/router.ts`, an entry
  in `ROUTE_META`, a priority in `src/bin/build-sitemap.ts` — plus a case in
  `resolvePage` if the path is nested or parameterized. The sitemap drift test
  and the "every `<loc>` resolves 200" test fail until all of it is done.
- **No user-agent sniffing, ever** — everyone gets the same enriched HTML.
- **Unknown paths are hard 404s.** A soft-200 SPA shell for a bad URL is an SEO
  bug; `resolvePage` returns the status and the fallback handler honours it.
- **No-JS bodies come from the same `data/*.json` rows the page renders**
  (`src/server/noJsBody.ts`). Never write server-only copy into one: a crawler
  must not index text a visitor cannot see. `[<kind>:<id>]` markers resolve
  through the SHARED `src/share/markers.ts` grammar — never re-implement the
  marker regex on one side (that shipped raw `[summon:…]` markers into served
  bodies once; see scaffolding-plan.md §12).
- **Share-card meta layers on top** of page meta, never under it
  (`ogInject.ts` runs last) — a shared build must unfurl as that build.

Canonical convention **[playbook]**: lowercase path, no trailing slash except
root — and the server 301s every other spelling to it.

### JSON-LD **[playbook]**

- Static `WebApplication` in index.html
- `/characters` + `/weapons`: `CollectionPage` (client-side)
- Doll/weapon detail pages: `WebPage` with `about` (client-side)
- `BreadcrumbList`: **server-side only** (`injectBreadcrumbLd`) — the client
  emits none, so the two can't duplicate each other
- Escape via `escapeJsonLd` — `web/src/jsonLd.ts` on the client,
  `src/server/htmlHead.ts` on the server (prevents `</script>` breakout)

---

## 7. Backend and data flow

### The rule **[playbook]**

All game data runs client-side. The ONLY data the app reads at runtime is
build-imported JSON. **Never add a `fetch()` for game data.**

### The GFL2 hop

Game data is owned by Postgres (dandegate sync). The sync step exports the
four committed artifacts the web imports — `data/dolls.json`,
`data/weapons.json`, `data/keys.json`, `data/effects.json`. Fresh checkouts
build from the committed JSONs without a DB. After any sync that adds dolls
or weapons, the exporter rewrites the JSONs and the next build picks the new
entities up automatically.

### Cross-references inside the data

- Doll ↔ weapon imprint: by weapon `id`
- Keys/effects ↔ doll: by `dollId`
- `[effect:<uuid>]` markers inside skill/weapon text: resolved against
  `effects.json` (id → effectName) through ONE helper in `data.ts` — never
  ad-hoc regexes at call sites
- Slugs ↔ ids: the exporter guarantees slug uniqueness (§4.3 of the plan);
  `data.ts` owns the lookup

### User-data backend (auth + saved profiles)

The ONE runtime API: a Hono server in `src/server/` (`npm run dev:server`,
prod `npm run serve`) that serves `dist/` with SPA fallback, runs Discord
OAuth (`/auth/discord/login` + `/auth/discord/callback`), and exposes the
kind-keyed saved-profiles API (`/api/me`, `/api/profiles`). It is the
playbook §8.2 shape, ported — bearer token in localStorage (`gfl2.auth`),
delivered via URL **fragment** (`#nsat=…`) so tokens stay out of server logs
and Referer; sessions are stateless HMAC tokens (`src/server/session.ts`).
`web/src/auth.ts` is the single client contract — every page imports it,
never hand-rolled fetches. Profile `code` blobs are opaque base64url; the
server validates shape/size only and the DB never interprets them. In dev
the vite proxy forwards `/auth` + `/api` to localhost:4173, so CORS never
bites. Do not invent a second auth or persistence pattern.

---

## 8. Naming conventions **[playbook]**

| Thing        | Convention                                      | Examples                                                  |
| ------------ | ----------------------------------------------- | --------------------------------------------------------- |
| Components   | PascalCase, named export                        | `DollFilters`, `DollCards`                                |
| Hooks        | camelCase, `use` prefix                         | `useDollFilter`, `useWeaponFilter`                        |
| CSS classes  | kebab-case, semantic, shared prefix per feature | `.dollcard-*`, `.teambuilder-*`, `.unit-*` (detail pages) |
| Constants    | UPPER_SNAKE or camelCase const                  | `PHASE_COLORS`, `CLASS_OPTIONS`                           |
| Types        | PascalCase                                      | `Route`, `Doll`, `FilterState`                            |
| Data modules | kebab-case                                      | `data.ts` (the typed JSON layer)                          |
| Page files   | PascalCase + `Page` suffix                      | `CharactersPage.tsx`, `DollPage.tsx`                      |

---

## 9. Do / Don't **[playbook, adapted]**

### Do

- Use `var(--token)` for every color
- `loading="lazy"` on grid images; `tabular-nums` on numeric columns
- Named exports; `as const` for fixed arrays; explanatory WHY comments
- Route new pages through `lazy()` + Suspense
- Degrade, never vanish — missing data gets one muted line
- Resolve `[effect:uuid]` markers through the shared `data.ts` helper
- Keep both regions: `regionTag` travels with every entity to the UI (badge),
  never silently dropped

### Don't

- No CSS modules / Tailwind / styled-components; no second CSS file
- No default exports; no state library; no React Router; no hash routes
- No `window.addEventListener('resize')`
- No `fetch()` for game data; no runtime DB/API access from `web/` except
  through the single `auth.ts` client (§7)
- No hand-written per-entity blurbs — everything derives from `data/*.json`
- No hand-editing `data/*.json` — they are sync output
- No font files until infographics exist (§3)
- No thumbnail/downscale pipeline until real art exists (§5)
- `as any` only for JSON-import casts

---

## 10. Recipes

### Adding a new page **[playbook]**

1. `web/src/YourPage.tsx` — `<div className="app page-class">` + header
2. `router.ts` — add to `Route` (+ `ROUTES` if not parameterized) + path
   mapping
3. `main.tsx` — `lazy()` import + a branch in the route switch
4. `SiteChrome.tsx` — nav or hamburger entry
5. `styles.css` — a `/* ---- Your page ---- */` section, shared class prefix
6. `src/share/pageMeta.ts` — a `ROUTE_META` entry (unique keyword-rich title,
   description, breadcrumb label). This is the only place the copy lives; the
   client head sync and the server injection both read it (§6)
7. `src/bin/build-sitemap.ts` — a priority (omit ONLY for a page that must not
   be indexed, and give it `noindex` in `pageMeta.ts` `NOINDEX_KEYS`), then
   `npm run sitemap` and commit the XML
8. `src/server/pageMeta.ts` — a `resolvePage` case if the path is nested or
   parameterized; a crumb trail in `breadcrumbFor` if it isn't `Home › leaf`
9. JSON-LD if the page has structure worth declaring (§6)
10. `src/server/noJsBody.ts` — a no-JS body if the page is a crawl target

### Adding a filter axis

1. Option array in `DollGrid.tsx` (`{ id, label, icon? }`, `as const`)
2. State + toggle in `useDollFilter`; predicate inside the `useMemo` filter
   (OR within the row, AND with the others)
3. A `FilterRow` in `DollFilters`; icon file into `public/gfl2-icons/` if
   available (pill fallback is automatic)
4. Include the new set in `anyActive` / `clearAll`

### After a sync adds dolls/weapons

`npm run sync` rewrites `data/*.json`; commit the diff. Grids and detail pages
pick new entities up at the next build; run `npm run sitemap` (or any
`vite:build`, which runs it) and commit `web/public/sitemap.xml` too — the
drift test fails otherwise. Mirror the new art with `npm run icons` so the new
pages get a per-URL embed image instead of the generic card.

---

## 11. Not yet adopted (port from the playbook when needed)

| Playbook section                                                   | Adopt when                                                                                    |
| ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- |
| §6.3–6.6 server meta injection, 404/cache policy                   | ADOPTED 2026-08-17 — see §6 and scaffolding-plan.md §12                                       |
| §7 no-JS bodies                                                    | ADOPTED 2026-08-17 for `/characters`, `/weapons` and both detail pages; `/keys` still pending |
| §10 image pipeline (thumb tiers, stepped downscale, crop constant) | real portrait/icon assets land (portraits are fetched from the CDN at render time — §12)      |
| §8.4 worker pool                                                   | heavy client compute exists                                                                   |

---

## 12. Server-rendered share images (playbook §9, adopted 2026-08-16)

Share cards are rendered server-side and served through immutable,
content-addressed URLs:

- **Renderers** — `src/infographics/core/` is platform-free: cards draw
  against a structural `Canvas2DLike` (no stroke API — borders are two
  fills) and never import @napi-rs/canvas or sharp. `core/buildCard.ts`
  (1200×630 OG card per doll build) and `core/teamCard.ts` (squad
  composition, height grows per filled slot). Colors mirror the §3 CSS
  tokens one-for-one (`core/theme.ts`); `PORTRAIT_CROP_TOP = 0.16` in
  `core/canvas2d.ts` MUST stay in sync with `--portrait-crop-top: 16%` in
  styles.css. Every card carries the mandatory `drawBrandMark` watermark
  (no text parameter — no renderer can ship an unmarked image).
- **Node side** — `src/infographics/node/render.ts` is THE ONLY entry
  (`fonts.ts` first: unregistered fonts render blank SILENTLY, and
  `assertFontsLive()` proves ink before every render). Portraits come from
  the CDN via `node/portraits.ts` (sharp → raw RGBA → putImageData; skia's
  loadImage silently no-ops drawImage on the owner's macOS), bounded LRU,
  null → placeholder box, never throws.
- **Image API** — `GET /api/v1/img/build.png|team.png?b=<code>` (or
  `?id=<public profile uuid>`; the stored code is expanded BEFORE hashing).
  Decode → validate slugs/ids against the committed `data/*.json` → 302
  (`no-cache`) to `/api/v1/img/cache/<kind>.<hash>.png`, served
  `public, max-age=31536000, immutable`. The hash is
  `sha256(RENDERER_VERSION|kind|canonical JSON)[0:16]`
  (`src/infographics/cacheKey.ts`) — content-addressed because Discord
  caches embed images by URL forever: every handed-out URL must resolve to
  the same pixels permanently. Bump `RENDERER_VERSION` on ANY visual
  change. Renders are single-flighted and written atomically into the
  gitignored `render-cache/`.
- **OG injection** — the static handler (`src/server/app.ts` SPA fallback +
  `ogInject.ts`) rewrites og:title/og:image/twitter tags for
  `/builder/<slug>?b=|id=` and `/team-builder?b=|id=` when the payload is
  well-formed; junk params get the default head. No UA sniffing.
- **Public share read** — `GET /api/profiles/:id/public` (no auth) reads
  only rows whose kind is in `PUBLIC_KINDS` (`['gfl2-share']`,
  `src/server/publicShare.ts`); everything else 404s indistinguishably, and
  the response never includes discord_id.
